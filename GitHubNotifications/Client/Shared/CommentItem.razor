<div class="list-group-item">
    <div>
        <a href="@Comment.uri">@Comment.title</a><br />
        <strong>@at@Comment.author</strong>&nbsp;@Comment.body -
        @if (@Comment.created.Day == DateTime.Now.Day)
        {
            @Comment.created.ToString("h:mm tt")
        }
        else
        {
            @Comment.created.ToString("MM/dd h:mm tt")
        }
    </div>
    @if (Comment.replies?.Count > 0)
    {
        <div class="list-group" style="max-width: inherit; overflow-wrap: anywhere;">
            @foreach (var reply in Comment.replies.OrderByDescending(c => c.created))
            {
                <div class="list-group-item">
                        <strong>@at@reply.author</strong> @reply.body -
                        @if (@reply.created.Day == DateTime.Now.Day)
                        {
                            @reply.created.ToString("h:mm tt")
                        }
                        else
                        {
                            @reply.created.ToString("MM/dd h:mm tt")
                        }
                </div>
            }
        </div>
    }
</div>

@code {

    [Parameter]
    public Comment Comment { get; set; }

    private string at = "@";

}